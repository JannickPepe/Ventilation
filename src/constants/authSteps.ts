/** Step labels shown during login (e.g. in step progress UI). */
export const AUTH_STEPS_LOGIN = [
  'Validating…',
  'Signing in…',
  'Redirecting…'
] as const

/** Step labels shown during register (e.g. in step progress UI). */
export const AUTH_STEPS_REGISTER = [
  'Validating…',
  'Creating account…',
  'Signing you in…'
] as const
